# Documentation staleness audit — 2026-06-24 (v1 shipped)

**Status:** FINDINGS COMPLETE, EXECUTION PENDING. Produced by a 5-agent parallel sweep over ~57 docs
(~11.5K lines) the day v1 shipped. This is the actionable tidy-up plan — hand to pilot → devSys-2 / a
grunt to execute, or do it as planner doc-work. Most fixes are one-line headers, not rewrites.

## Reality baseline (what to judge staleness against)
- v1 SHIPPED live at deltos.blackgate.studio. Canonical branch = `ui-refresh`; `phase-0-foundation` is the
  DEPRECATED old deploy branch (main cwd still on it). Verify code refs in `.claude/worktrees/ui-refresh/`.
- Auth = username+password + optional 2FA + recovery-phrase reset. Passkey/WebAuthn/signed-challenge/PRF were
  ABANDONED in the 2026-06-17 pivot — all such design docs are HISTORICAL.
- Shipped & live: notebooks (create/name/switch/move/delete→uncategorize), All-Notes synthetic default
  (notebookId nullable, no stored default), search, note-history + History panel + undo/redo, the Deck custom
  keyboard (voice/inline-math/rich-embeds/spellcheck/custom-dict), mobile swipe actions (delete/copy=RIGHT,
  move=LEFT→notebook sheet), desktop 3-region shell + drag-and-drop note→notebook, UI visual refresh
  (themes light/dark/system, fonts, icons, appearance picker, δ brand), live note-refresh, idle/tab sync-pause,
  the sync blip (solid green/ring/yellow/grey) + tap-flush-reload.
- REMOVED (refs are stale): #68 kbprobe probe; the old inline "Move to notebook" picker. PARKED: #62 / slash-palette.

## HIGH — living docs materially stale (UPDATE, real work)
- **PLAN.md** — top "CURRENT STATE (2026-06-20)" block + the Phase-1-building tables are a shipped-milestone
  stale; passkey done-gate language; Search/history/visual-refresh listed as parked/remaining (all shipped);
  "editor engine open decision" (decided = ProseMirror). FIX: fold the stale CURRENT STATE into the historical
  `<details>` pattern the file already uses, write a fresh v1-shipped top block, mark-done the shipped items.
  LEAVE the dated decision log (append-only history; SHAs there are correctly point-in-time).
- **README.md** — §Status/Roadmap lists Search/visual-refresh/history/Settings as in-flight/parked (shipped);
  "Done & live" list stops pre-Deck (missing Deck, swipe-move, All-Notes, desktop DnD, link tooling); Repo Layout
  says latest migration "0009" (actual = 0012) and the client `src/` dir list omits deck/theme/icons/plugins/styles.
  FIX: move shipped→done, add the missing features, fix the migration + dir lines. (Stack/Architecture/Auth
  sections are accurate — LEAVE.)

## MEDIUM — historical docs that MISLEAD (one-line ARCHIVE-NOTE header, do NOT rewrite the body)
Header pattern: `> **Historical — v1 shipped 2026-06-24. This is the <spec/gate/plan> as of <date>; preserved as record, not current status.**`
- **v1-dg-cap-gate-record.md** — reads "HELD" + "v1-BLOCKING E4 fix-in-progress" → looks like v1 never shipped. Header CLOSED (worst offender of the gate set).
- **v1-done-gate-acceptance-checklist.md** — reads gate-open + "DGT-2 RED". Header CLOSED.
- **phase-0-foundation.md** — `Phase: 0` title framing. Header historical.
- **phase-1-constraints.md** + **phase-1-vertical-slice.md** — header historical PLUS a "superseded by
  conflict-as-version" pointer on the PIN-SYNC-3/4 fork-to-new-ID model (it contradicts the shipped
  conflict-as-version model — authoritative doc = `v1-shell-and-conflict-versions.md`).
- **v1-shell-and-conflict-versions.md**, **v1-shell-conflict-acceptance-matrix.md**, **stream-a-acceptance-checklist.md**
  — SPEC-READY/DRAFT/REFERENCE status + all-unchecked boxes read in-progress. Header shipped/satisfied.
- Pre-pivot auth design docs — one-line "Historical — pre-pivot passkey/signed-challenge design, abandoned
  2026-06-17, superseded by username+password (see auth-pivot-scope-map.md)":
  `docs/design/account-identity-strawman.md`, `stream-a-auth-contracts.md` (says "STABLE — build against them" =
  worst offender), `stream-a-auth-strawman.md`, `stream-a-auth-secSys-review.md`, `secSys-account-identity-review.md`,
  `part1b-option-a-autounlock-plan.md` (dead PRF auto-unlock plan, never built).
- **KICKOFF.md** — add "superseded: passkey→password (D-auth), editor=ProseMirror (D1)"; LEAVE body.
- **DECISIONS.md** — D5 (passkey device-revocation posture): append "⚠️ superseded by the 2026-06-17 pivot;
  recovery phrase is now a reset token, not the crypto root." LEAVE the rest.
- **brainstorm.md** — L303-308 identity section ("passkey … used always", present tense) → supersede note;
  mark editor-engine decided (L361). Rest (plugins/E2EE/location/notebook-loadouts) is legitimately FUTURE — LEAVE.

## MEDIUM — feature specs stale/contradictory (UPDATE)
- ⚠️ **swipe-actions-note-list.md** — says LEFT swipe = reserved/Pin (LIVE: LEFT = Move→notebook sheet) AND
  `deletedAt` soft-delete (LIVE: Fork P `sys:trashedAt` property). Update the gesture model; archive-note the delete mechanism.
- ⚠️ **settings-screen.md** L71-73 — "there is no theming system yet / appearance out of scope" flatly contradicts
  the shipped Appearance picker (it lives IN Settings). Delete/update that note. Status SPEC-READY→shipped.
- **ui-backbone-notebooks.md** — "always a default notebook / delete→Trash / all-notes deferred / note belongs to
  exactly one notebook" all REVERSED by the All-Notes synthetic-default model. Archive-note pointing to
  `all-notes-synthetic-default.md`.
- **custom-keyboard.md** — L316 treats kbprobe/#68 cleanup as pending (it's removed); status line understates
  shipped phases (1-2 + voice + spellcheck + dict live). Update.
- **ui-visual-refresh.md** — "Deploy 3 NOT started / Lanes 3+4 SPEC-READY / wire Undo-Redo" all shipped;
  navSys-2→navSys-3 handoff block obsolete. Mark-done + relabel historical build-log.
- **all-notes-synthetic-default.md** — SPEC-READY→shipped; L39-40 describes the OLD inline move picker → update to swipe-sheet + DnD.
- **search.md**, **note-history-and-undo.md**, **drag-gesture-hook.md**, **auth-pivot-password.md** — SPEC-READY status lines (shipped) → mark-done.
- **auth-disclosure-copy.md** L11-12 — "pending nod" dependency flag resolved (phrase-clears-2FA locked) → remove.
- **inline-math.md** — superseded by inline-formulas.md (archive-note). **inline-formulas.md** — framework + math + hexcolor shipped → mark-done; dice legitimately future.
- **rich-embeds.md** — rungs 0-2 + `/api/unfurl` shipped → mark-done; rung-3 + the SSRF/throttle "before real users" gates legitimately OPEN → LEAVE those.
- **ui-tokens-and-fonts.md / ui-lane5-appearance-and-brand.md / ui-lanes34-editor-toolbars-markdown.md / notes-list-display.md** — status-line stale → mark shipped/historical. (lanes34: as-built shipped `EditorControlStrip.tsx`, not the spec's `EditorToolbar.tsx`.)
- **bottom-nav-mobile.md** — status shipped; note mobile is now top-bar + BottomNav(default mode) with BottomNav SUPPRESSED in deck-custom; cross-ref custom-keyboard.md §4.

## DELETE
- `docs/design/client-account-isolation-audit.md` — true superseded DUPLICATE of `docs/specs/client-account-isolation-audit.md`
  (the specs version cites it by hash, recommends Option B, which shipped in `db/accountScope.ts`).

## LEAVE (accurate or legitimately historical — do NOT touch)
- CLAUDE.md (binding, current). `docs/design/auth-pivot-scope-map.md` (accurate pivot record).
  `part2-conflict-version-data-model.md` (shipped as designed). `secSys-cross-account-sweep.md`.
  `harness-prod-permission-findings.md`. All spikes (S1/S2/S3 findings + spike-S* specs — throwaway research).
  `stream-d-notes-accountid-scope-checklist.md` (already COMPLETE). `docs/design/ui-refresh/README.md` (FROZEN
  design packet — its Graphite-default / nav divergences are intentional, tracked in the lane specs; do NOT edit to match reality).

## Genuinely-actionable (NOT doc staleness — route as tasks)
- `auth-pivot-acceptance-matrix.md` L86-92: AP-T8's two 2FA-change revoke-all regression tests may never have been
  written (code correct + secSys-verified, but no labeled belt). Verify; add if missing.
- Code-doc drift (ui-refresh worktree): `db/accountScope.ts` header still says "PROTOTYPE … not shipped" though
  isolation IS shipped; stray `/kbprobe` comment in `plugins/formula/formulaPlugin.ts:27`. Both trivial cleanups.
