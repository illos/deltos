# Workspace packages link by protocol only; never silently pull from the registry.
link-workspace-packages=true
prefer-workspace-packages=true
# Keep the store strict so a missing workspace dep fails loudly instead of hoisting.
shamefully-hoist=false
