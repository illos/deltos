{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "types": ["vite/client", "vite-plugin-pwa/client"],
    "noEmit": true
  },
  // Workers run in the WebWorker lib, not DOM — typechecked separately (tsconfig.sw.json).
  "include": ["src"],
  "exclude": ["src/sw.ts", "src/deck/spellcheck/spellWorker.ts"]
}
